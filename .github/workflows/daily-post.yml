name: Daily Blog Post

on:
  schedule:
    - cron: "0 0 * * *" # 毎日 日本時間 朝9時 (UTC 0:00)
  workflow_dispatch: # 手動実行ボタン用

permissions:
  contents: write

jobs:
  generate-and-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Generate blog post
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: npx tsx scripts/generate-post.ts

      - name: Commit and push changes
        run: |
          git config --global user.name "UrbanGray Bot"
          git config --global user.email "bot@urbangray.com"

          # 【修正箇所1】パスを修正（client/src... ではなくルートの posts/ を指定）
          git add posts/

          # 変更があるかチェック
          if git diff --staged --quiet; then
            # 【修正箇所2】console.log はエラーになるので echo に変更
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "📝 Auto-generated blog post [skip ci]"
          git push
